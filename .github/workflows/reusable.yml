name: Resualbe Deploy
on: workflow_call                     
jobs:
  reusable_workflow_job:
      runs-on: ubuntu-latest
      steps:
        - name: Deploy
          run: |
            ls ${{ github.workspace }}
            echo "Deploying..."